# Pinned exactly as resolved by `pip freeze` on 2026-08-19 against Python 3.14.7.
# Phase 2 additions (dbt-core, dbt-duckdb) pinned 2026-08-21 — see docs/versions.md.
# Do not hand-edit versions here; see docs/versions.md for the resolution/compat
# probe behind each pin and docs/decisions.md for why Python 3.14.7 was chosen.

annotated-types==0.8.0    # pydantic dependency
cachetools==6.2.6         # pyiceberg[sql-sqlite] dependency
certifi==2026.7.22        # requests dependency (TLS root certs)
charset-normalizer==3.5.1 # requests dependency
click==8.4.2               # pyiceberg CLI dependency
duckdb==1.5.5              # local read engine for the Iceberg warehouse; 1.5+ line, see docs/versions.md
fsspec==2026.7.0           # pyiceberg[pyarrow] filesystem abstraction
idna==3.19                 # requests dependency
markdown-it-py==4.2.0      # rich dependency (pyiceberg CLI output)
mdurl==0.1.2                # markdown-it-py dependency
mmh3==5.2.1                 # pyiceberg partitioning hash function
pyarrow==25.0.1             # Iceberg table I/O and Arrow interop
pydantic==2.13.4            # pyiceberg config/schema models
pydantic_core==2.46.4       # pydantic dependency
Pygments==2.21.0            # rich dependency
pyiceberg==0.11.1           # Iceberg table format read/write, spec v2
pyiceberg-core==0.7.0       # pyiceberg native extension
pyparsing==3.3.2            # pyiceberg expression parsing dependency
pyroaring==1.1.0            # pyiceberg[sql-sqlite] roaring-bitmap dependency
python-dateutil==2.9.0.post0 # strictyaml dependency
python-dotenv==1.2.3        # loads SOCRATA_APP_TOKEN from .env
requests==2.34.2            # Socrata HTTP client
rich==14.3.4                 # pyiceberg CLI output formatting
six==1.17.0                  # python-dateutil dependency
SQLAlchemy==2.0.52           # pyiceberg SqlCatalog (SQLite) backend
strictyaml==1.7.3            # pyiceberg config file parsing
tenacity==9.1.4              # pyiceberg retry logic
typing-inspection==0.4.4     # pydantic dependency
typing_extensions==4.16.0    # pydantic/pyiceberg typing backport
urllib3==2.7.0                # requests dependency
zstandard==0.25.0             # pyiceberg Parquet/metadata compression

# --- Phase 2: dbt modeling (pinned 2026-08-21, Python 3.14.7) ---
dbt-core==1.12.3              # dbt modeling framework
dbt-duckdb==1.11.0             # dbt adapter: DuckDB + Iceberg reads via the iceberg extension
# Transitive dependencies of the above two, all as resolved by pip:
agate==1.9.1
attrs==26.1.0
Babel==2.18.0
colorama==0.4.6
daff==1.4.2
dbt-adapters==1.24.5
dbt-common==1.39.0
dbt-core-experimental-parser==2.0.0b2
dbt-extractor==0.6.0
dbt-protos==1.0.565
deepdiff==8.6.2
importlib_metadata==9.0.0
isodate==0.7.2
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
leather==0.4.1
MarkupSafe==3.0.3
mashumaro==3.17
metricflow==0.212.0            # bundled with dbt-core; local MetricFlow used in Phase 4
more-itertools==10.8.0
msgpack==1.2.1
networkx==3.6.1
opentelemetry-api==1.44.0
orderly-set==5.5.0
packaging==26.3
parsedatetime==2.6
pathspec==1.0.4
protobuf==6.33.6
python-slugify==8.0.4
pytimeparse==1.1.8
pytz==2026.3.post1
PyYAML==6.0.3
RapidFuzz==3.14.5
referencing==0.37.0
rpds-py==2026.6.3
snowplow-tracker==1.1.0
sqlglot==30.17.0
sqlparse==0.6.0
tabulate==0.10.0
text-unidecode==1.3
zipp==4.1.0

# --- Phase 4: MetricFlow semantic layer (pinned 2026-08-23, Python 3.14.7) ---
# metricflow (above, already pinned as a dbt-core transitive dependency) is the
# query engine; dbt-metricflow is the separate CLI package that actually
# provides the `mf` command — installed into the SAME main venv as dbt-core,
# not a separate one like Soda (C3.1): `pip install --dry-run` resolved
# cleanly with zero conflicts against dbt-core<1.13,>=1.11 / dbt-duckdb 1.11.0,
# verified before installing for real. Both metricflow and dbt-metricflow are
# License-Expression: Apache-2.0, confirmed via `pip show` (C4.1).
dbt-metricflow==0.14.0        # the `mf` CLI — local/OSS query path, no dbt Cloud
halo==0.0.31                  # dbt-metricflow dependency (CLI spinner)
log-symbols==0.0.14           # halo dependency
spinners==0.0.24              # halo dependency
termcolor==3.3.0              # halo dependency
update-checker==0.18.0        # dbt-metricflow dependency
